executable(
  'RunLua',
  'RunLua.cxx',
  include_directories: inc,
  dependencies: [
    lua_dep,
    util_dep,
  ],
)

executable(
  'CoLua',
  'CoLua.cxx',
  include_directories: inc,
  dependencies: [
    lua_dep,
    lua_pg_dep,
    event_dep,
    util_dep,
  ],
)
